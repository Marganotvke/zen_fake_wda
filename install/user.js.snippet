// Copy these lines into your Zen profile user.js (or set via about:config).
// Required for transparent web content area option in the mod:
// user_pref("browser.tabs.allow_transparent_browser", true);

// Fake transparency is controlled by the mod pref zen.fake_transparency.enabled.
// Wallpaper URL is normally written by scripts/windows/sync-wallpaper.ps1 into prefs.js.
